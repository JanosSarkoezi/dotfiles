#compdef run

_run()
{
    local curcontext="$curcontext" state line
    typeset -A opt_args

    _arguments -C \
        ':command:->command' \
        '*::options:->options'

    case $state in
        (command)
            local -a subcommands
            subcommands=(
                'tmux:starts tmux with own configuration.'
                'venv:starts python virtualenv.'
                'mount:mounts several remote directories.'
                'umount:unmounts all remote directories.'
            )
            _describe -t commands 'run' subcommands
        ;;
        (options)
            case $line[1] in
                (tmux)
                    __run-tmux
                ;;
                (venv)
                    __run-venv
                ;;
                (mount)
                    __run-mount
                ;;
            esac
        ;;
    esac
}

__run-venv()
{
	local curcontext="$curcontext" state line
	typeset -A opt_args

	_arguments -C \
		':command:->command' \
		'*::options:->options'

	case $state in
		(command)
			local -a subcommands
			subcommands=(
				'main:Start the main envitonment.'
				'ninja:Start the ninja environment.'
			)
			_describe -t commands 'run venv' subcommands
		;;
	esac
}

__run-tmux()
{
	local curcontext="$curcontext" state line
	typeset -A opt_args

	_arguments -C \
		':command:->command' \
		'*::options:->options'

	case $state in
		(command)
			local -a subcommands
			subcommands=(
				'monitor:Start tmux with monitoring windows and panes.'
				'work:Start tmux with work windows and panes.'
			)
			_describe -t commands 'run tmux' subcommands
		;;
	esac
}

__run-mount()
{
	local curcontext="$curcontext" state line
	typeset -A opt_args

	_arguments -C \
		':command:->command' \
		'*::options:->options'

	case $state in
		(command)
			local -a subcommands
			subcommands=(
				'user:Mounts remote user directories.'
				'home:Mounts remote home directory.'
			)
			_describe -t commands 'run mount' subcommands
		;;
	esac
}
